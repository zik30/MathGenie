export const BASE_URL = import.meta.env.VITE_API_URL;

export const routes = {
    home: '/',
    subjects: '/subjects',
    questions: '/questions',
    tests: '/tests',
    test: '/test',
    profile: '/profile',
    login: '/login',
};
